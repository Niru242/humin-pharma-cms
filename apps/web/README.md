# Frontend (Next.js)

The frontend UI lives in the sibling workspace:
`/opt/lampp/htdocs/humin-pharma/frontend`

This folder is a placeholder in the monorepo structure.
The frontend connects to the API at `http://localhost:4000/v1` (configured in `src/lib/api.ts`).

When the monorepo is fully consolidated, the frontend source will move here.
For now, development continues in the existing workspace.
